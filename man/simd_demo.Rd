% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hello.R
\name{simd_count_nonzero}
\alias{simd_count_nonzero}
\alias{simd_set_backend}
\alias{simd_info}
\title{Count non-zero bytes with Rust SIMD kernels and C dispatch}
\usage{
simd_count_nonzero(x)

simd_set_backend(
  backend = c("auto", "scalar", "sse2", "avx2", "avx512", "neon")
)

simd_info()
}
\arguments{
\item{x}{A raw vector.}

\item{backend}{One of \code{"auto"}, \code{"scalar"}, \code{"sse2"}, \code{"avx2"},
\code{"avx512"}, or \code{"neon"}.}
}
\value{
The number of non-zero bytes in \code{x}.

The selected backend name, invisibly.

A named list with character-vector \code{compiled} and \code{supported}
backend sets, the runtime \code{selected} backend, and logical fields for
Rust-reported compiled and CPU features.
}
\description{
This is a Rust/C/R multiversioning demo. The SIMD kernels and CPU feature
probes live in Rust, while the C layer only caches a function pointer and
dispatches to the best Rust backend whose compiled feature and runtime CPU
feature are both available. The package still builds one R shared library;
it does not create one shared library per SIMD backend.

\code{"auto"} selects the fastest available Rust backend from the intersection of
compiled features and runtime CPU features. \code{"scalar"} is always available and
is useful for correctness checks and scalar-vs-SIMD benchmarks.
}
\examples{
simd_count_nonzero(as.raw(c(0, 1, 2, 0, 3)))
simd_set_backend("scalar")
simd_set_backend("auto")
simd_info()
}
